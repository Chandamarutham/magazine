# Copy this file to .env and fill in values before running locally.
# Vite requires env vars to start with VITE_

# AWS Configuration for Form Data Submitter
VITE_AWS_REGION=your-aws-region
VITE_COGNITO_IDENTITY_POOL_ID=your-region:YOUR-IDENTITY-POOL-ID-HERE

# API Gateway Endpoints - Production
VITE_API_BASE_URL=https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com/production
VITE_API_ERROR_ENDPOINT=https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com/production/error
VITE_API_ADVERTISE_ENDPOINT=https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com/production/advertise
VITE_API_SUBSCRIBE_ENDPOINT=https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com/production/subscribe
VITE_API_QUERY_ENDPOINT=https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com/production/query

# API Gateway Endpoints - Pre-Production (for troubleshooting)
VITE_API_PRE_PROD_BASE_URL=https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com/pre-production
VITE_API_PRE_PROD_ERROR_ENDPOINT=https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com/pre-production/error
VITE_API_PRE_PROD_ADVERTISE_ENDPOINT=https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com/pre-production/advertise
VITE_API_PRE_PROD_SUBSCRIBE_ENDPOINT=https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com/pre-production/subscribe
VITE_API_PRE_PROD_QUERY_ENDPOINT=https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com/pre-production/query

# Form Data Submitter Configuration
# Set this to 'true' to use pre-production endpoints (bypasses Cognito authentication)
# Useful for troubleshooting backend issues directly
VITE_USE_PRE_PRODUCTION=false

# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Replace YOUR-API-ID with your actual API Gateway ID (e.g., 6vzjagr1kh)
# 3. Replace YOUR-REGION with your AWS region (e.g., ap-south-1)  
# 4. Replace YOUR-IDENTITY-POOL-ID-HERE with your Cognito Identity Pool ID
# 5. Run: npm run validate-config to check your setup
#
# Example values:
# VITE_AWS_REGION=ap-south-1
# VITE_COGNITO_IDENTITY_POOL_ID=ap-south-1:12345678-1234-1234-1234-123456789012
# VITE_API_ERROR_ENDPOINT=https://abc123xyz.execute-api.ap-south-1.amazonaws.com/production/error

# Security Notes:
# - Do NOT commit a real .env with secrets to git
# - Keep your actual .env file local only
# - Use GitHub Secrets for production deployment